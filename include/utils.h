#ifndef UTILS_H
# define UTILS_H

# include <stdio.h>

t_string	build_path(t_string path, t_string file_name);

void		ft_print_n(char c, size_t n);
int			string_compare_ignore_case(t_generic a, t_generic b);

#endif // !UTILS_H
